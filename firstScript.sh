# first script

brew install nodejs npm vim tmux 
sudo npm install n -g
sudo n stable
sudo npm install -g prettier markdown-it

sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

