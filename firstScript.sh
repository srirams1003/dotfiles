# first script

sudo apt update
sudo apt install zsh nodejs npm vim tmux xsel
sudo npm install n -g
sudo n stable
sudo apt install curl wget git

sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

